const server = require( "../server.js" )
const supertest = require( "supertest" )
const requestWithSupertest = supertest( server )

describe("Get React App", () => {
    it('GET / should return the React app', async () => {
        const res = await 
    })
})